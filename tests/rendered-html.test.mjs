import assert from "node:assert/strict"; import test from "node:test";
async function render(){const workerUrl=new URL("../dist/server/index.js",import.meta.url);workerUrl.searchParams.set("test",`${process.pid}-${Date.now()}`);const{default:worker}=await import(workerUrl.href);return worker.fetch(new Request("http://localhost/",{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("renders role selector and conventional sign in",async()=>{const response=await render();assert.equal(response.status,200);const html=await response.text();assert.match(html,/QishloqMed AI/);assert.match(html,/Choose a workspace/);assert.match(html,/Enter as Mobile Nurse/);assert.match(html,/nurse@qishloqmed\.demo/);assert.doesNotMatch(html,/signin-with-chatgpt|codex-preview|react-loading-skeleton/)});
